---
layout: default
title: A Short Bio
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>CV Tools Progress Bar Example - Rakshit Soral</title>
    <meta name="viewport" content="width=device-width" />
    <style>
        /* Reset and base */
        html, body {
            margin: 0; padding: 20px; 
            font-family: 'Lato', Helvetica, Arial, sans-serif;
            font-size: 11pt;
            color: #222;
            background: #ccc;
        }
        /* Container simulating one page */
        .page {
            width: 210mm;
            min-height: 297mm;
            background: #fff;
            margin: 0 auto 5mm auto;
            padding: 15mm 20mm;
            box-sizing: border-box;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
        }
        /* Section Title */
        .sectionTitle h1 {
            font-family: 'Oswald', Helvetica, Arial, sans-serif;
            font-weight: 400;
            font-size: 18pt;
            color: #da2d2d;
            margin-bottom: 12px;
            text-transform: uppercase;
        }
        /* Tools section wrapper */
        .sectionContent {
            max-width: 180mm;
        }

        /* Progress bar container */
        .progress-bar-container {
            /* no extra margin here */
        }

        /* Tool label */
        .tool-label {
            font-weight: bold;
            font-size: 11pt;
            color: #da2d2d;
            margin-top: 12px;
            margin-bottom: 6px;
            text-transform: none; /* keep normal casing */
        }

        /* Progress bar background track */
        .progress-bar {
            background-color: #e0e0e0;
            border-radius: 6px;
            height: 14px;
            width: 100%;
            box-sizing: border-box;
            margin-bottom: 10px;
            page-break-inside: avoid;
            /* Prevent breaking inside for print */
        }

        /* Progress bar fill */
        .progress {
            height: 100%;
            background-color: #da2d2d;
            border-radius: 6px 0 0 6px;
            transition: width 0.5s ease;
        }

        /* On screen hover effect (optional, no print effect) */
        @media screen {
            .progress-bar:hover .progress {
                background-color: #b02929;
            }
        }

        /* Print styles */
        @media print {
            body, html {
                margin: 0; padding: 0;
                background: #fff;
                font-size: 10pt;
            }
            .page {
                box-shadow: none;
                width: 210mm;
                min-height: 297mm;
                margin: 0 auto 5mm auto;
                padding: 15mm 15mm;
                page-break-after: always;
            }
            /* Avoid page breaks inside progress bar blocks */
            .progress-bar-container, .progress-bar, .progress {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="page" role="main">
        <section>
            <div class="sectionTitle">
                <h1>Tools</h1>
            </div>
            <div class="sectionContent">
                <div class="progress-bar-container">
                    <div class="tool-label">HubSpot</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 90%;"></div>
                    </div>

                    <div class="tool-label">Mailchimp</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 85%;"></div>
                    </div>

                    <div class="tool-label">Google Analytics 4 (GA4)</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 80%;"></div>
                    </div>

                    <div class="tool-label">Apollo.io</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 75%;"></div>
                    </div>

                    <div class="tool-label">LinkedIn Campaign Manager</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 70%;"></div>
                    </div>

                    <div class="tool-label">ChatGPT</div>
                    <div class="progress-bar">
                        <div class="progress" style="width: 95%;"></div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</body>
</html>
