---
layout: default
title: Blog
description: Blogs and Tips on Life, Web Development, Demand Gen, and Marketing.
pagination:
    data: collections.posts
    size: 1000
    alias: posts
---

{# Set default counter start based on total posts plus 1 #}
{% set postsListCounter = postsListCounter or collections.posts.length %}
<ol reversed class="postlist" style="counter-reset: start-from {{ postsListCounter + 1 }}">
  {% for post in collections.posts | reverse %}
    <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
      <a href="{{ post.url | url }}" class="postlist-link">
        {% if post.data.title %}
          {{ post.data.title }}
        {% else %}
          <code>{{ post.url }}</code>
        {% endif %}
      </a>
      <time class="postlist-date" datetime="{{ post.date | dateToISOString }}">
        {{ post.date | readableDate }}
      </time>
      {% for tag in post.data.tags %}
        {% if collections.tagList and collections.tagList.indexOf(tag) != -1 %}
          {% set tagUrl = "/tags/" + tag + "/" %}
          <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
        {% endif %}
      {% endfor %}
    </li>
  {% endfor %}
</ol>
