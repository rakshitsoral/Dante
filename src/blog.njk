---
layout: default
title: Blog
description: Blogs and Tips on Life, Web Development, Demand Gen, and Marketing.
pagination:
    data: collections.posts
    size: 1000
    alias: posts
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "Stylish Blog Page" }}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        {# Blog rows: for 3 columns, chunk posts into rows of 3 #}
        {% set postsList = collections.posts | reverse %}
        {% for row in postsList | batch(3) %}
            <div class="blog-row">
                {% for post in row %}
                    <div class="blog">
                        <div class="blog-image">
                            {% if post.data.featured_image %}
                                <img src="{{ post.data.featured_image | url }}" alt="{{ post.data.title }}">
                            {% else %}
                                <img src="/path/to/default-image.png" alt="No image">
                            {% endif %}
                            <div class="date">
                                {% if post.date %}
                                    {{ post.date | date("MMMM D, YYYY") }}
                                {% else %}
                                    No Date
                                {% endif %}
                            </div>
                        </div>
                        <div class="blog-content">
                            <h2>{{ post.data.title }}</h2>
                            <p>
                                {% if post.data.description %}
                                    {{ post.data.description }}
                                {% else %}
                                    {{ post.templateContent | striptags | truncate(120, true, "…") }}
                                {% endif %}
                            </p>
                            <a href="{{ post.url | url }}">Read More</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
    <footer>
        <div class="footer-container">
            <p>&copy; {{ "now" | date("YYYY") }} My Blog. All Rights Reserved.</p>
            <div class="social-links">
                <a href="#">Facebook</a>
                <a href="#">Twitter</a>
                <a href="#">LinkedIn</a>
            </div>
        </div>
    </footer>
</body>
</html>
